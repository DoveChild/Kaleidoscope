


#ifndef UNIFORM_H
#define UNIFORM_H

// InitializeSeed(): Initialize the random number seed
void InitializeSeed();

// Uniform() generate a random number in the range i to j inclusive
int  Uniform(int, int);

#endif